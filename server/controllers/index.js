const authentication = require("./authentication");
const registration = require("./registration");
const playerState = require("./playerState");

module.exports = { registration, authentication, playerState };
